<UserControl x:Class="BookStore.Presentation.Views.TopMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStore.Presentation.Views" 
             xmlns:viewmodels="clr-namespace:BookStore.Presentation.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="700" Background="{StaticResource MainAppColor}">
    <Grid>
        <TextBlock Text="{Binding SelectedStore.Name}" Margin="4,4,0,2" FontSize="18" FontWeight="Bold"/>

    </Grid>
</UserControl>
