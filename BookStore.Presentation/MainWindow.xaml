<Window x:Class="BookStore.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookStore.Presentation"
        mc:Ignorable="d"
        xmlns:View="clr-namespace:BookStore.Presentation.Views" 
        xmlns:local1="clr-namespace:BookStore.Presentation.ViewModels"
        Title="Book Store Manager" Height="400" Width="700">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <View:TopMainView Grid.Row="0"/>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <View:MenuView Grid.Column="0" VerticalAlignment="Stretch"/>
            <View:InventoryView Grid.Column="1" VerticalAlignment="Stretch"/>
        </Grid>
    </Grid>
</Window>
